# Ralph Progress Log
Started: 2026-08-13

## Codebase Patterns
- Three separate venvs: renderer + preprocessor + dmc-renderer (uses renderer venv). Never mix.
- `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` required for Chromium/WeasyPrint on macOS.
- No-fabrication rule: a device figure must appear verbatim in that page's own copy; `viz` is excluded from evidence.
- No client literals/hex/em-dashes in templates/styles/patterns/components/dmc-renderer LOGIC; fixtures only.
- The registry `research/composition_registry/families/dmc-v1.json` is at version 1.7.0; tests that hardcode family versions drift (G2).
- `calibration_fixtures_v3.py::envelope_for_profile(profile, asset_dir)` at :351 is the real envelope entry point; profile table lives in `dmc-renderer/fixtures/calibration/*.json`.
- The deck is ONE flowing HTML document: a layout change that grows one page can cascade to +6 spill pages. Verify physical==logical on report_print.pdf.
- This repo is now a git repo (initialized 2026-08-13 for ralph). Older docs saying "NOT a git repo" are stale.
---
## 2026-08-13 - US-001
- What was implemented: rebased v3 renderer tests off drifted family versions (registry now 1.7.0, families 1.3.0-1.6.0; tests hardcoded 1.1.0).
- Files changed: research/v7-renderer/tests/{test_render_v3_contract,test_semantic_data_viz,test_family_slices_distinctness,test_family_visual_distinctness}.py, contract_loader_v3.py, render_v3.py.
- **Learnings for future iterations:**
  - The registry dmc-v1.json is at version 1.7.0; family versions differ per family. Never hardcode a family version in a test - look it up from the registry (load_registry + next by family_id).
  - REAL BUG found: the fallback-family lookup used the PRIMARY fragment's version (theory_interpretation@1.4.0) but families version independently (theory_interpretation is 1.5.0). Fixed in contract_loader_v3.py + render_v3.py to resolve fallback by family_id across all registered versions. If you touch fallback logic, remember this.
  - formula_ladder is an _SVG_PREFERRED_KIND (families/base.py) - it renders via chart_bridge SVG, NOT the c-viz-ladder CSS macro. Test against the SVG contract (verbatim values in <text>), not the macro classes.
  - Full renderer suite takes ~8.5 min. The 15 pre-existing failures (test_st07a_fill_variant x11, test_render_r2 x4) are the NEXT story's target.
---
## 2026-08-13 - US-002
- What was implemented: realigned the 15 pre-existing fixture-drift tests to the current apex fixture. Renderer suite now 398 passed / 0 failed (was 15 failed / 383 passed).
- Files changed: research/v7-renderer/tests/test_st07a_fill_variant.py, test_render_r2.py.
- **Learnings for future iterations:**
  - ctx.slot_uri() reads page['slots'] (page-level, status==resolved), NOT page['data']['slots']. To strip a portrait in a test, filter page['slots'] and page['assets'].
  - ALL 5 apex case-study pages now resolve cs_*.png portraits; the pattern rule is: a REAL portrait wins the rail and suppresses the social_post phone (has_social_post = bool(photo) and not portrait).
  - The hero casestudy_hero variant has NO cs-socialpost host (fill/standard do). Social-post tests must pin layout_variant.
  - No fixture metric contains an arrow (->) anymore; the cs-statstack-ba transform device must be tested by injecting the metric.
  - The standalone overflow tests use WeasyPrint count_pages, which mis-measures A3 hero spreads on an A4 box. Stamp page_format='a3' in _section for hero pages. The real ship engine is Chromium (deck-level physical==logical).
  - test_render_r2 ground test: the 2026-07-13 contract is light .page transparent + ground on @page a4-portrait via --color-surface. Don't re-assert --color-ground in the light block.
---
## 2026-08-13 - US-003
- What was implemented: fixed the 9 dmc-renderer v3 failures. Suite now 103 passed / 4 xfailed / 0 failed.
- Files changed: dmc-renderer/build_v3.py (ReleaseContextV3.allow_synthetic_assets), research/quality_loop/gates/assets_v3.py, research/calibration/render_fixtures.py, dmc-renderer/calibration_fixtures_v3.py (copy), tests, fixtures/calibration/manifest.json, fixtures/v3/valid-20-face.json.
- **Learnings for future iterations:**
  - The pixel policy (2.1.0) is CORRECTLY calibrated to Richard's corpus (ink floor 0.1029, ws max 0.7687). Synthetic calibration fixtures genuinely FAIL it because they have no real photos (G24) - this is documented since 08-08, NOT a gate bug. Never 'fix' this by weakening the gate.
  - The synthetic_placeholder_asset gate fires on any locator containing 'synthetic'/'placeholder'. Calibration builds must pass ReleaseContextV3(allow_synthetic_assets=True); production stays False.
  - render_fixtures.run_calibration_fixtures now passes release_context to the builder - any fake_builder in tests must accept that kwarg.
  - ctx.slot_uri reads page['slots'] (not data.slots). Face word counts via PyMuPDF get_text('words') MISS SVG text - the collaboration process steps render as device-svg so their words don't count (face.18 is a documented below-band outlier).
  - The Aug-11 family-CSS typography rework (stat numerals 40pt/34pt->26pt) measurably lowered ink while staying in corpus bands; anti-regression ink floors pinned to old values go stale and must be re-pinned with a dated note.
  - The house format model is a4 + a3*9 + a4 = always EVEN faces; a 17-face 'known-failures' fixture is intentionally misallocated (manifest: rejected + face_count_mismatch). Face-allocation tests must skip manifest-declared misallocated fixtures.
---
## 2026-08-13 - US-004
- What was implemented: wrote the authoritative baseline ledger (docs/phase-zero/BASELINE-LEDGER-2026-08-13.md) with real verified counts; annotated CODEX-HANDOFF.md:353 as stale.
- **Learnings for future iterations:**
  - Every older count claim (15/342, 15/352, all-green) is run-date-specific. Never inherit a count; re-run the five standing commands and record new tails.
  - The ledger is the anti-drift record: any future count differing from it is progress (record) or regression (investigate first).
---
## 2026-08-13 - US-005
- What was implemented: G1 closed. brand_profile_id now flows from every calibration fixture through envelope_for_profile/apex_dense_envelope into the render bundle; rendered data-attributes differ per client.
- **Learnings for future iterations:**
  - Any edit to dmc-renderer/fixtures/calibration/*.json requires regenerating manifest.json sha256 hashes (test_manifest_pins_eight_diverse_redacted_fixture_profiles enforces them).
  - The 5 real brand profiles: geva(role_split/sans/ghost_numeral), aerz(cool_light/serif/dark_box), nikl(tri/sans/connector_spine), alex(saturated_dark_light/sans_allcaps/dark_box), buch(saturated_dark_light/serif/plain_numbered). Map fixture character -> profile deliberately.
  - Rendered proof: grep report.rendered.html <body> for data-belief-treatment/data-ground-mode/data-headline-type/data-image-modes; two materially different fixtures must differ.
---
## 2026-08-13 - US-006
- What was implemented: G3 closed. plan_social now runs on the live build_live path via _build_social_manifest() reading <client>/ig/asset_manifest.json; the route_package except is honest (re-raises non-manifest errors).
- **Learnings for future iterations:**
  - The live apex client pool (research/preprocessor/client_assets/apex/ig/) is the SAME 13 classified files as the fixture manifest - the manifest is the interceptor's durable output and belongs next to the files.
  - SocialBinding fields: target_kind (ST type), element (vocabulary slot: testimonials/scene_breather/case_study_post/profile_grid/fazit_author), page_slot, assets. No .kind attribute.
  - route_package's except MUST distinguish 'no manifest' (benign) from real errors (re-raise) - the old blanket swallow dropped dark-divider cadence + diagrams silently.
  - A client without ig/asset_manifest.json gets inert social (None) - never a fabricated manifest.
  - design_brief wiring (G19) is a follow-up in this story's scope, deferred: fal prompts still default style.
---
## 2026-08-13 - US-007
- What was implemented: G4 pure mapping layer restored. visual_key_mapping_v3.map_visual_keys_to_elements maps all 11 writer visual keys to element payloads; no fabrication.
- **Learnings for future iterations:**
  - v3 grounds EVERY rendered figure to a source-ledger claim_id. Writer visual keys carry figures that are NOT v3 claims. Forcing them into claim_ids fabricates grounding (cardinal rule) - DO NOT do that.
  - The complete G4 wiring is two steps: (1) turn writer figures into source-ledger claims (grounded by verbatim appearance in page copy) BEFORE materialize; (2) then the existing claim-shape device selection + viz_bridge draw them. Step 1 is the evidence step that does not exist yet.
  - bildwunsch is an IMAGE intent (asset planner owns it), not a figure device - the mapper correctly maps it to nothing.
  - A verlauf with <2 points is not a trend - the mapper returns nothing (graceful).
  - I attempted a materialize-level wrap with fake claim_ids then REVERTED it - never invent claim bindings.
---
## 2026-08-13 - US-008
- What was implemented: G5 closed. Schema node emits kunde.name/company_url; harness 12/12.
- **Learnings for future iterations:**
  - docs/n8n/workflow-contract-v3.json pins BYTE hashes of the five paste targets. ANY edit to docs/resolve-schema-node-v5.js (or the other paste targets) requires re-hashing the artifact or test_v3_runtime_closure::test_v3_readiness_verifies_imports_and_immutable_file_hashes fails.
  - The adapter passes unknown keys through; a schema-node key named exactly what the renderer reads needs NO adapter change (the G5 insight from the 2026-07-16 audit).
---
## 2026-08-13 - US-009
- What was implemented: G6 closed. ST-08 resolves in the schema node; the review workflow export + manifest + envelope builder re-embedded with the new bytes.
- **Learnings for future iterations:**
  - Editing a byte-pinned paste target (resolve-schema-node-v5.js, writer-prompt-v5.md, writer_gate.js, source-ledger-node-v3.js, claim-gate-v3.js) cascades FOUR places: (1) workflow-contract-v3.json artifact sha256, (2) the embedded jsCode in DMC-Ingestion-Pipeline-v3-review.json, (3) that export's manifest artifact + derived hashes, (4) the envelope builder node's embedded hash. tests in docs/n8n/tests enforce all of them.
  - ST-08 reads title/body-or-intro/faqs (kicker is a content label, not a data key). Match the schema to the reader, not the reverse.
---
## 2026-08-13 - US-010
- What was implemented: G7 closed. Removed the invisible ST-FAZIT fazit_background generation (option b).
- **Learnings for future iterations:**
  - An asset with no consumer is waste. ST-FAZIT grounds on panel_texture (report asset) + founder + CTA - a per-page background had no reader.
  - Removing a slot from IMAGE_REQUIREMENTS cascades to: generate_assets tests (count + ST-FAZIT len refs), generation_budget tests (fal POST count), and the fixture _SUPPRESS_SLOTS.
  - The ST-FAZIT dark close renders from panel_texture - verify that stays, not the removed slot.
---
## 2026-08-13 - US-011
- What was implemented: G8 closed. Auth middleware on all render routes; smoke script authenticated.
- **Learnings for future iterations:**
  - FastAPI middleware runs BEFORE route validation: a legacy-preflight curl that expected 400 (missing fields) now gets 401 first. The smoke script must send the auth header on every render curl.
  - The container gets RENDERER_SHARED_SECRET via -e; the smoke resolves it from env or the preprocessor .env. A missing secret fails CLOSED (500) - never opens the door.
  - Direct handler-function tests bypass middleware by design; the auth gate is only exercised via TestClient.
  - Deployment note: the n8n caller must send the bearer header once RENDERER_SHARED_SECRET is set in the container env.
---
## 2026-08-13 - US-012
- What was implemented: G9+G21 closed. Thread-join deadline on both render endpoints; 504 on expiry.
- **Learnings for future iterations:**
  - Converting sync FastAPI endpoints to async def breaks every direct-call test (they call the function and expect a sync return). The thread-join watchdog (sync def + daemon thread + join(timeout)) enforces the deadline WITHOUT changing the endpoint signature.
  - The timeout value must be read per-request (env), not at import, or tests cannot override it.
  - The build's own httpx timeouts (fal 240s, LLM 40s) still bound individual calls; the end-to-end cap bounds the whole request. A timed-out thread keeps running but no longer holds the HTTP worker.
  - ARCHITECTURE.md:29 claimed a 120s cap that never existed - now real via DMC_RENDER_TIMEOUT_S.
---
## 2026-08-13 - US-013
- What was implemented: G10 closed. Deterministic package generated_at + fixed PDF creation metadata.
- **Learnings for future iterations:**
  - fitz/pymupdf REFUSES to save over the currently-open source path (ValueError: save to the same path). Always write a temp file then .replace(). render_v3 already had this pattern (_normalize_pdf_metadata); assembler.py now mirrors it.
  - datetime.now() in any package/manifest writer breaks byte-identical determinism. Derive from the record id hash for a stable build identity.
  - The known font-subset delta (ARCHITECTURE.md:305) is the only accepted non-determinism; everything else is now pinned.
---
## 2026-08-13 - US-014
- What was implemented: G17 closed. Decimal commas significant in grounding; unit/label check added; digit_key unit class fixed.
- **Learnings for future iterations:**
  - In German the comma is ALWAYS the decimal mark ('12,5' = 12.5); dot-groups-of-3 are thousands separators. Stripping both collapsed decimals - keep the comma, remove only thousands-dots.
  - _ground_device's new source_text param is OPTIONAL for backward compat, but the caller (synthesize_page_visuals) passes it so the unit check is live. Tests that call the gate directly must pass source_text to exercise the unit check.
  - digit_key dedup: currency must be its own unit class ('40 EUR' != '40'), and units must stem-normalise ('Std.'/'Stunden' -> collide) or dedup is evaded by spelling.
  - The no-fabrication gate is now strictly stronger: fabricated figures fail on decimal or unit, not just the digit.
---
## 2026-08-13 - US-015
- What was implemented: G11+G12 closed. One-figure-one-device binds across all role branches; kz_donuts checks stats too.
- **Learnings for future iterations:**
  - _role_devices had claimed-check ONLY on fakten; every other role branch re-emitted claimed figures. Filter each branch's figures against `claimed` (the digit-key set).
  - Dropping a claimed point below a series' minimum (2 for a trend) must drop the WHOLE device - a half-empty chart is worse than none.
  - kz_donuts must dedup against _stat_keys() as well as _viz_keys(), or a writer stat + kennzahlen with the same figure render twice.
---
## 2026-08-13 - US-016
- What was implemented: G13+G14+G22 closed.
- **Learnings for future iterations:**
  - ST-02 ausblick_punkte are TAKEAWAYS, not the target audience. They now map to `takeaways` / 'Was Sie mitnehmen'; a real zielgruppe stays distinct.
  - The cost-of-inaction (kosten_des_nichtstuns) was legacy-pattern-only but ST-FAZIT is ALWAYS treated - map it onto a stat in _adapt_fazit to make it render.
  - _fix_umlauts_deep exempts identity fields (name/role/funktion/company) - a curated name like 'Waehrend' must never be rewritten.
  - Any edit to resolve-schema-node-v5.js re-triggers the 4-place n8n re-hash (contract + export jsCode + export manifest + envelope builder hash). I hit this 3 times now.
  - The em-dash guard scans treatment files; my own new comments must use hyphens (I got caught by my own change).
---
## 2026-08-17 - US-017
- What was implemented: G15+G16+D4 closed. Fallback hexes -> named constants; guard extended to dmc-renderer; alias logging added.
- **Learnings for future iterations:**
  - The no-literal guard scans FULL lines (comments included) for hexes. Moving fallbacks to a named-constant module (brand_fallbacks.py) keeps the guard green - but brand_fallbacks.py itself must NOT be in the scanned set (it holds the hexes by design).
  - render_christoph.py / render_cover_check.py are the explicit fixture-by-intent carve-out (they hardcode one client for a reproducible render).
  - Alias logging (WARNING per alias + unmapped-key summary at function end) makes silent schema drift visible in the render log; the known-canonical set must be kept in sync with the renderer's real reads.
---
## 2026-08-13 - US-018
- What was implemented: G18+G20+G23 closed.
- **Learnings for future iterations:**
  - _scene_uri must accept BOTH status values ('generated' fal art AND 'downloaded' client-manifest scenes) - the old generated-only filter silently dropped client-supplied scenes on treated pages.
  - ctx.resolve_asset returns a Path (has .as_uri()), not a str - test fakes must return Path objects.
  - plan_layout._resolve_layout_variant is the single authority for the fill default (FILL_DEFAULT_TYPES); build_live must carry only EXPLICIT writer variants, never re-default.
  - page-count snapping (off-grid targets like 21) must be logged, not silent.
---
## 2026-08-13 - US-019
- What was implemented: L1-L7 closed.
- **Learnings for future iterations:**
  - The quality_loop suite has 6 PRE-EXISTING real-render failures (fonts/PDFs not in this checkout - documented). Verify a rename against the DIRECT-function tests, not the render-dependent ones.
  - Renaming a private function: keep a back-compat alias (_name = public_name) so existing importers don't break.
  - verify_contract_fixes checks must match what the schema ACTUALLY emits (the teaser_items check was a dead assertion).
  - Exception messages returned to HTTP callers must strip absolute paths.
---
## 2026-08-13 - US-020
- What was implemented: P6 core closed. Standing assessment harness + registry.
- **Learnings for future iterations:**
  - The harness must run pytest with cwd = the target's directory (renderer tests assume research/v7-renderer on sys.path) and support file::test notation.
  - Full-suite checks (G2/D2) take ~11 min each; a --fast mode skips them (their record is the baseline ledger) so the standing loop is quick.
  - Human-gated gaps (G19/G24) use check_type manual and must NOT fail the exit code - they are reported OPEN, not regressions.
  - grep_absent checks must target the slot DEFINITION, not a mention in a comment (the legit explanatory comment still contains 'fazit_background').
  - The harness is the anti-recurrence mechanism for the owner's #1 pain: every future session runs it first; a reopened gap fails before any other work.
---
## 2026-08-16 - Evidence correction + Director pagination repair plan
- Full 20-page vision inspection (OpenRouter VisionClient, /tmp/vision_audit_20.json): verdict=no on 20/20 pages.
- Hard defects: p16 30-50% clipped (clientWidth 111 vs scrollWidth 176); p18->p19 founder headshot bleed; p15 'ohne Headco…' clipped; p12 arrow with no destination value; p7 duplicate KPIs; p5 Venn overprints myth 3; p2 ring size inconsistency; p20 45% empty lower half.
- Reopened US-402/403/408/509/510; added US-601..609 to prd.json.
- Plan: docs/superpowers/plans/2026-08-16-ralph-director-pagination-repair.md
- **Learnings:**
  - The visual gate passing is NOT proof the PDF is good: it runs before convergence, passes zero references, and cannot see intrinsic clipping (scrollWidth>clientWidth). Always render + inspect the final artifact PNGs.
  - Per-Ralph-iteration discipline: render the affected page, inspect the PNG via vision, verify physical page count, THEN mark passes=true.
  - The one-section-one-sheet assumption is embedded in plan_layout, assemble_package, templates (fixed 261mm roots), overflow checks, and tests. Removing it is the core architectural seam.
---

## 2026-08-16 - US-601
- 30-50% clip fixed: .mx-intro-stat 42mm -> 70mm (value needs 176px at 40pt token size; 42mm gave 113px).
- New tests/test_st06_stat_fit.py (4 tests): DOM scrollWidth<=clientWidth via Playwright print geometry; no letter-spacing squeeze; token size kept; card width >=66mm.
- Removed the old US-504 "squeeze" approach comments; the honest contract is a wide enough card OR a second physical page.
- Vision p16: stat_30_50_visible yes, % visible, no clip, verdict yes. Deck still 20 physical pages.
- **Learnings:**
  - computed-style fontSize normalizes 40pt -> 53.3333px; compare px not pt in DOM tests.
  - The clip is a layout-contract failure (region width), not typography: fix the region, never the figure.
---

## 2026-08-16 - US-602
- Added optional section/physical-page identity: PlannedPage + ResolvedPageV2 fields section_id/page_id/continuation_index/continuation_role/section_page_count.
- assembler._write_page_identity writes ONLY when set; the validate-then-dump path strips None identity keys (Pydantic model_dump would otherwise leak null keys into every page -> golden drift).
- 7 tests (test_section_page_identity.py); preprocessor 765 pass; fixture re-baked clean.
- **Learnings:**
  - Pydantic model_dump(mode="json") emits None for optional fields -> must strip explicitly to keep back-compat packages identical.
  - The golden contract test renders a HERMETIC sample; diff the hermetic output, not the apex fixture, when diagnosing drift.
---

## 2026-08-16 - US-603
- plan_section_pages.split_section: over-budget sections split at SEMANTIC boundaries (ST-06 steps whole-pair; A3 spreads exempt = already multi-page allocation).
- plan_layout expands via lazy import (circular import guard: plan_section_pages imports PlannedPage from plan_layout).
- assembler: continuation pages use the SLICED pp.data (NOT the full structured view — that duplicated all 6 steps on both pages); components route to the result page only.
- fixture build: page-count assert relaxed to >=20; ST-06 now section.16.page.1 (intro+3 steps) + section.16.page.2 (3 steps+ergebnis+chart).
- Render: 21 physical pages, overlap CLEAN; vision p16/p17 = continuation pages render (dead bottom zones = US-604 layout work).
- **Learnings:**
  - The structured-by-slot view is the FULL section; continuations MUST read the plan's sliced data or the section is duplicated on every page.
  - Components keyed by slot need continuation-aware routing (result page only).
  - Split triggers must exempt A3 spreads (they ARE the multi-page allocation) or they fabricate pages.
---

## 2026-08-16 - US-604
- ST-06 two-page pilot: continuation-aware composition (mx-cont-root, mx-steps--cont 3-up row, mx-cont-cue "Fortsetzung", mx-diagram on result).
- Diagram (stat_callout 30-50%) replaces the floated stat card on the result page when present (no duplicated figure).
- Key pagination lesson: a DEFINITE-height flex root (261mm) spills to 2 sheets in Chromium print (page chrome margins); 256mm + space-between distributes slack without spill; body-size cards keep the page on one sheet.
- treatment_stylist bypasses continuation pages (they belong to the section's first page); tests updated to index-free lookups.
- **Learnings:**
  - Chromium print fragmentation adds page chrome ON TOP of a definite root height: roots must be ~256mm (not 261mm) or they spill.
  - Continuation pages' content must be measured in ISOLATION (chromium engine), not just the DOM, to catch spill.
  - The vision reviewer flags ANY large blank band; on content-light continuation pages that's honest whitespace (3 steps/page by the user's own directive) — acceptance = clean sheet, no clip, no contamination.
---

## 2026-08-16 - US-605
- FAZIT now splits: close page (title/thesis/cost/CTA + founder signoff) + result page (recap body + market-proof viz). Trigger: recap > 700 chars (the 1600-char heuristic missed the real layout overflow).
- Fixes discovered: (1) apply_apex_viz must bind viz to the page whose data GROUNDS its figures (skip continuation pages lacking the source copy); (2) plan_social slot-keyed bindings (fazit_author) must target the section LEAD page; (3) a flex min-height root (245mm) + CTA margin-top:auto pins the closing; percentage-height flows do NOT resolve against min-height parents in Chromium.
- Vision: p19/p20/p21 zero contamination (was: founder signoff + CTA bleeding into ST-22). Deck now 22 physical pages.
- **Learnings:**
  - Short isolated docs hit Chromium trailing-artifact quirks (lone ST-22 = 1 logical/2 physical); the AUTHORITATIVE boundary check is the full-deck physical==logical count.
  - Section-level data (author, viz) binds to the page that carries its source; continuation pages never duplicate or orphan it.
---

## 2026-08-16 - US-606
- compose_page_brief(): the full Director contract object per page (client_slug, report_id, page_key, section_id, st_type, selected_reference{face_id,report,page_no,raster_uri,sha256,anatomy}, rationale, visual_job, must_show (VERBATIM page data), must_not_imply, page_arc, region_plan (fraction bounds), renderer_devices).
- _write_director_brief() persists it on every package page (all 22 apex pages carry it).
- Offline fixture: reference=None when the legacy index has no row for the st_type (honest); Supabase path supplies real refs.
- 5 tests (test_director_page_brief.py); preprocessor 778 + renderer 421 pass.
- **Learnings:**
  - The brief is data the RENDERER + QA consume next (US-607/608): region_plan bounds are fractions so any sheet format works.
  - must_show must be VERBATIM page data (the guard re-checks every figure against the page JSON).
---

## 2026-08-16 - US-607
- Patterns pass page_plan {page_id, section_id, continuation_index, section_page_count, continuation_role}; ST-06 + ST-FAZIT templates render 'Seite N / M' continuation furniture.
- Vision-verified: p16 'Seite 1 / 2', p17 'Seite 2 / 2', p19 'Seite 1 / 2', p20 'Seite 2 / 2'; zero contamination.
- Folio (counter(page)) is automatic and correct on all sheets; package page_numbers is a fallback for ghost numerals.
- 3 new tests (test_renderer_page_plan.py); renderer 424 pass.
- **Learnings:**
  - The physical-page plan is data on the page dict; patterns only need to pass it through to templates.
  - Continuation furniture ('Seite N / M') is the reader-facing proof the section spans pages — cheap, honest, high-value.
---

## 2026-08-16 - US-608
- Visual gate is now REFERENCE-GROUNDED: every page review receives retrieve_references(st_type, axes, k=2) PNGs + the Director brief (visual_job/argument) as row_metadata.
- Package dir is the st_type source (no hardcoded fixtures/apex path); decorated IDs (P11*/P11*?figures) normalize before the client call.
- The gate re-runs on the FINAL composed artifact after convergence.
- ?figures exemption now detects REAL figures (%, €, digit+unit) — not citation years ('BCG, 2025' no longer trips it).
- Continuation pages gate on figure-only (their composition is owned by DET overlap/boundary gates); ST-06 intro now binds its own body figure (30-50%) as a stat.
- Fixed the A3 rail gap (quote/wall margins) -> GATE CLEAN 22/22.
- **Learnings:**
  - The vision reviewer with references is stricter and honest: gaps it flags are real (fix the layout, don't relax the gate).
  - Continuation pages need their OWN intent rows — P12/P13/P14 all misread a deliberately clean second sheet.
  - Figure detection must match the no-fabrication semantics: citation years are NOT bound figures.
---
