# DMC Report: Current Build State (2026-06-20, pre-chat-clear HEAVY checkpoint)

## ★ STATUS NOW (2026-06-20) — READ THIS FIRST. Treatment system Phase 0/1 DONE; now mid-build on the AI decorative-asset (img2img) pipeline.

This supersedes the 2026-06-18/19 sections below (which remain valid as the plan + Phase-0/1 build detail). Written atom-level for a COLD restart after a chat clear. Memory companions (read all): [[treatment-library-state]], [[generative-pipeline-wiring-state]], [[ai-decorative-assets]], [[regressions-and-guardrails]], [[richard-design-system]], [[deck-breathing-layout-philosophy]], [[no-em-dashes]].

### A. TREATMENT SYSTEM — what is BUILT + GREEN (Phase 0 + Phase 1, ~42 targeted tests pass)
New files in `research/v7-renderer/`:
- `treatment_engine.py` — `@dataclass TreatmentData` (fields: st_type, css_template, page_role, eyebrow, headline, headline_accent, subhead, body[list], sections[{label,text}], stats[{value,label}], steps[{title,body}], list_items[list], quote{text,attribution}|None, credentials[list], image|None, images[list], viz, cta, **caption{name,role}|None** (added for the About founder), **result{text,figure,source}|None** (added for ST-06 ergebnis)) + `adapt(page,ctx)` (generic mapping + per-st_type `_ADAPTERS`; reuses `patterns.st_07a._SECTION_SPEC` + `_KICKER_BASE`; no-fabrication: `.get`/truthiness only) + `@dataclass(frozen) Treatment` (name, archetype, formats:frozenset, required_fields, needs_image, template, css_path, render_fn) + `TREATMENTS` registry + `register`/`get_treatment` + `meets_contract(td,treatment)` (`_is_present` emptiness rule) + `render(page,ctx,name)->Optional[PageFragment]` (None on unknown/unmet; render_fn else template+css; never swallows a real bug) + `candidate_fits`.
- `treatment_catalog.py` — registers the Treatment descriptors (editorial, horizontal_process, a4_vertical_timeline, a4_bi_dashboard, a4_metric_column, a4_two_stack, a4_dark_divider, a4_quote_portrait, a4_side_rail, a4_stacked_hero, a4_portrait_card, glass_card, split_portrait). Template/css paths convention: `treatments/<name>.html.jinja` + `styles/treatments/<name>.css`.
- `treatment_stylist.py` — `assign(pages,ctx)->[PageAssignment{index,slot,st_type,treatment,page_format,reason}]`: BYPASS_ST_TYPES={ST-01,ST-03,ST-31,ST-32,ST-07B}; per-st_type `_CANDIDATES` (best-fit-FIRST order); selection = best-fit-first + deck-wide used-set DEDUP + no-adjacent-repeat (relaxation order: prefer unused+non-adjacent → reuse → relax adjacency → never crash); A3 cap=3; `founder_identity(pages,ctx)` (first resolved `founder` slot path + first `author` dict — for image-led pages lacking their own portrait). Deterministic.
- `templates/treatments/` + `styles/treatments/` — editorial, horizontal_process, a4_vertical_timeline (authored). Guard `tests/test_no_literals_in_architecture.py` now rglobs templates/styles + scans treatment_*.py + a U+2014 em-dash scan over treatment dirs.
- Tests (all green): test_treatment_pagesize, test_treatment_engine, test_treatment_dispatch, test_treatment_stylist, test_treatment_wiring, test_treatment_slice, test_treatment_qc, plus the extended no-literals + panel-contrast.
Modified:
- `assembler.py` — named `@page a3-landscape`/`@page a4-portrait` rules (chrome re-declared in BOTH engine branches; the named rules + `.page.format-a3{page:a3-landscape;min-height:0;border:none}` / `.page.format-a4{page:a4-portrait}` routing classes were the Phase-0 make-or-break, PROVEN on the gs-flattened PDF). `render_package(..., treatments=False)` runs the stylist after `load_package` (ctx built ONCE now, reused) gated by the param; `_render_one_page(page,ctx,assignment,treatments,*,warnings)` (treatment-first, raise-safe fallback to `_render_legacy_page`, reports used_treatment/used_format only when the treatment actually rendered); `_section(...,treatment,page_format)` stamps `.page.treatment-<name>.format-<a3|a4>`; founder-identity injection: when a page assigned a needs_image treatment has no resolvable own portrait, the assembler injects a synthetic resolved `founder` slot + `author` onto the page dict before render (so `adapt` resolves the founder for the About page). Skips the WeasyPrint per-page overflow check for treated/A3 pages.
- `render.py` — `--treatments` flag (default OFF = the rollback; this tree is NOT git). When ON, Stage-9 convergence is skipped (it grades vs A4 st_type refs and would false-flag A3/treatment pages).
- `tokens/base.tokens.json` — added `--space-7: 16mm`, `--space-8: 24mm` (the scale was only 1..6; an undefined `--space-7` was silently breaking layouts — see regressions).

### B. THE TWO PILOT PAGES — as-built (heavily revised through user feedback; this is the QUALITY BAR for Phase 2)
The slice helper `tools/_ts15_slice_render.py` renders pages `[0, 2, 15]` (cover bypass + ST-05 About + ST-06) into `output/ts15_slice/`. (NOTE: the bypass ST-01 cover SPILLS to a 2nd physical A4 sheet in this short 3-page slice — a Chromium short-doc/mixed-@page artifact; PNG numbering is p1 cover, p2 cover-spill, p3 EDITORIAL, p4 FRAMEWORK. The FULL 20-page deck is clean: `test_treatment_slice.py::test_full_deck_no_spill` asserts 20==20.)
- **editorial → ST-05 About (A3-landscape).** Left cream field: eyebrow + two-tone serif headline + lede + an "IN ZAHLEN" metrics zone (a teal **30-50% donut** via `viz_proportion` + `100+`/`€200k+` KPI callouts) + a bottom closing-thesis paragraph (`td.body[1:]`) anchored via space-between so the field FILLS top-to-bottom. Right full-bleed NAVY column: the **founder portrait** (sourced via the founder-identity fallback) + caption (name/role from `author`) + a "Vertrauen von" credential wall (`td.credentials` = partners), filling the column top-to-bottom (portrait 56% / creds 44% explicit flex bases). 
- **horizontal_process → ST-06 (A3-landscape, NEW treatment).** Header (two-tone headline + lede) + a full-width horizontal 6-step process flow on a teal spine (numbered nodes 01..06, ALL six bodies render) + a bottom dark result band with the **30-50% efficiency gauge** + the verbatim PwC source (from `td.result`/ergebnis).
- ST-22 stays `a4_vertical_timeline` (A4). The A3 set is now `[2 (About), 15 (framework)]`.
- **QC gate built (the user demanded the SYSTEM catch defects, not their eye):** `tools/qc_dead_space.py` — a maximal-empty-rectangle detector that flags large empty panels / dead bands (panel-color rect >7%, ground/cream rect >22%). `tests/test_treatment_qc.py` gates the two treated slice pages. RUN IT (+ panel-contrast + no-literals) before showing any treatment page. KNOWN GAP: it does NOT catch dark-text-on-dark-panel overlap (see the open defect in E).

### C. USER DESIGN DECISIONS this session (the WHY — do NOT relitigate)
1. Editorial belongs on the **About page (ST-05)**, not the ST-07A case study ("Make About the hero") — because the rich editorial layout (donut + 100+/€200k+ + partner wall + founder) is About-page data. The case study gets a simpler proof layout later.
2. The framework page (ST-06) = a **horizontal process flow** (A3, full width) + a 30-50% result KPI.
3. Premium textures/footer/decorative touches must be **AI-GENERATED via the fal pipeline, NOT hand-rolled procedural CSS** (the user corrected this explicitly). I had been building a procedural-CSS marble footer; that was the wrong layer.
4. Use **whole AI decorative assets** (the user OVERRODE the analysis's "materials-only/hybrid" recommendation; still: hard bans on text/logos/data/faces remain).
5. Use **image-to-image (img2img / fal edit), not text-to-image** — seed each call with a clean brand-correct canvas so the model can only refine, not invent (the anti-slop mechanism). FAL_KEY is SET; user said **no rotation needed**.

### D. REGRESSIONS + ROOT-CAUSES FIXED this session (durable; see [[regressions-and-guardrails]])
- **Chromium print bug: `flex: 1 1 auto` (flex-grow) inside an absolutely-positioned column collapses to ZERO height in the PDF** (the editorial credential wall vanished → "empty navy box"). FIX: explicit complementary flex bases (portrait 56% / creds 44%), never flex-grow in an abspos column for print.
- **Undefined `--space-7` token** (scale was 1..6) resolved to empty → `gap: normal` → zero gutter (the donut/KPI "cramped" bug; also silently broke st_03.css). FIX: added space-7/8 to `tokens/base.tokens.json`.
- **Editorial dead-space (`height:100%` resolving to auto → field shrink-wrapped to ~62%).** FIX: `.ed-grid` flex column + `.ed-field{flex:1 1 auto}` fills the definite grid height.
- **`a4_vertical_timeline` dropped step 6's body** (the vertical-timeline template bug); the new `horizontal_process` loops ALL steps.
- **3 stale `test_render_r2` tests** (st_09 numbered-block→viz-mega, st_05 primary→ink reground, footer @bottom-left→@bottom-right, + the pruned fal status_quo_scene) FIXED (these assert intentionally-superseded behavior). 
- **3 stale `test_st07a_fill_variant`/`test_viz_host_st07a` tests** are PRE-EXISTING (social_post-vs-portrait + c-viz-ring; verified fail on the pristine fixture) — NOT yet fixed, low priority.
- **FIXTURE EDIT (frozen-fixture trap, note for restart):** `fixtures/apex/resolved_package.json` founder slots repointed `*_founder.png` (landscape 1.56) → `*_founder.jpg` (portrait 0.82) on ST-01 + ST-FAZIT (the .jpg is the premium portrait per design memory; improves the cover too). ALSO added a `report_assets` entry `{slot_id:"panel_texture", image_type:"panel_texture", path:"assets/panel_marble.png"}`. These are MANUAL edits to the frozen fixture — they REVERT if `build_package.py` is re-run. Originals backed up in `/tmp/ts_about_backup/resolved_package.json.ORIGINAL` (volatile) — re-apply if regenerated.

### E. OPEN DEFECT (MUST FIX) + the procedural-marble footer status
- **UNFIXED: the editorial closing-paragraph collides with the curved navy footer band's LEFT crest** (dark navy text sitting on the dark navy band = illegible). Found by controller crop-inspection; the `qc_dead_space` gate does NOT catch text-on-panel overlap (a QC gap worth closing: add a "dark text over dark panel" check). I was mid-fix (reduce/flatten the editorial footer band height + curve amplitude so the paragraph clears it) when the user pivoted to AI assets.
- **The curved-marble FOOTER is currently PROCEDURAL CSS** (`fixtures/apex/panel_marble.py` generates a navy marble PNG via `stages.procedural_texture.marble` with LOCKED params x_period=0.85 y_period=1.5 turb_power=2.6 vein_sharpness=1.5 seed=4 + navy stops; the editorial/horizontal_process CSS draw an SVG-mask curve + a double teal hairline + fill it with the marble via a `--panel-marble` CSS var resolved from the `panel_texture` report asset). Per the user pivot (C.3), this procedural marble is now a STOPGAP / the SEED SOURCE for img2img — the FINAL fill should be the AI-generated stone. Do NOT keep iterating the procedural marble as the final.

### F. ★ AI DECORATIVE-ASSET PIPELINE (img2img) — the ACTIVE work, MID-BUILD. Full detail in [[ai-decorative-assets]].
- Ran a forensic Workflow over the 4 reference decks + 26 mockups (result: `/private/tmp/.../tasks/w24825vzt.output`). FINDING: Richard-premium = "two materials in conversation (cream paper + navy stone), joined by precise hairline trim, on one depth plane"; the only AI-worthy part is the MATERIAL (geometry stays renderer-vector). User chose whole-assets anyway; we proceed with img2img which makes whole-assets safe.
- **BUILT (no-fal prep, verified --dry):** `research/preprocessor/tools/gen_decorative_assets.py` — img2img generator. Endpoint `fal-ai/nano-banana-pro/edit` (confirmed via fal docs: takes `image_urls` list = base64 data URIs OK, `prompt`, `num_images`, `output_format`, `resolution` up to 4K; NO negative field → bans folded into the prompt). 4 assets: `report_navy_stone` (1:1), `report_navy_footer` (auto), `report_fazit_field` (3:4), `report_cream_paper` (1:1). Each prompt is an EDIT instruction ("transform this → keep the navy + glow exactly → add photoreal stone, matte, low contrast" + the full ban list incl no-teal/cyan/blue). `--dry` prints prompts + makes NO call. Writes to `fixtures/apex/assets/<name>.png`. Loads FAL_KEY from env (`set -a; . ./.env >/dev/null 2>&1; set +a` — never echo).
- **SEEDS (the anti-slop key):** `/tmp/seeds/seed_navy_field.png`, `seed_footer_band.png`, `seed_fazit.png`, `seed_cream.png` — clean brand-correct canvases (navy #2E3E50 + the soft glow already placed via a radial gradient + a faint procedural-marble hint; cream #F6F3ED). Generated by an inline script using `stages.procedural_texture.marble` (PYTHONPATH=research/preprocessor). Regenerate from the recipe in [[ai-decorative-assets]] if /tmp is cleared.
- **`design_brief` AUTHORED** in `fixtures/apex/brand_input.json` (the structural root-cause fix for the original slop: without it, `build_image_prompts.py` returned {} → Nano Banana dumped raw German page text). Block: visual_style_summary + mood + imagery{style,subjects,treatment,lighting,avoid[full ban list]} + color_usage (cream/navy locked, teal forbidden in materials). `build_package.py:201` reads `design_brief=brand_input.get("design_brief")`.
- **`generate_texture()` in `stages/generate_assets.py` is STILL A STUB** (line ~420). The focused `gen_decorative_assets.py` script bypasses it (does its own fal edit POST). Properly un-stubbing generate_texture for the full pipeline is a follow-up.
- **CONSUMPTION MOCK** rendered for the user: `/tmp/mock_assets.html` → `/tmp/mock_assets.png` shows all 4 asset roles on one A3 (cream=ground, navy stone=stat panel + footer fill, fazit field=closing block; renderer draws the curve/hairline/type on top). The user is gut-checking it.
- **PENDING (the immediate next action when chat resumes):** FIRE the 4 fal img2img generations (`cd research/preprocessor; set -a; . ./.env >/dev/null 2>&1; set +a; .venv/bin/python tools/gen_decorative_assets.py`) → 4 paid 2K gens → VIEW each output, reject any with text/teal-cast/banding/hard-light (re-seed/re-prompt if so) → wire the renderer treatments to consume them (replace the `--panel-marble` procedural source with the AI navy stone; set the cream ground from `report_cream_paper`; un-suppress fazit) → re-render the slice + the showcase mockups → QC (`qc_dead_space` + view) → show the user. The user was about to approve firing.

### G. SUBAGENT CRASH LESSON (operational)
TWO long `Agent`/general-purpose subagents (~14-21 min, the footer build + the fal-prep) hit `API Error: socket connection closed unexpectedly` and returned NO final report (partial work on disk; they DID back up first, which saved state). LESSON: for this work, prefer SMALL/SHORT dispatches or drive directly; long multi-file+iterate subagents are crash-prone here. Always instruct "back up each file BEFORE editing".

### H. NEXT STEPS (ordered, for the cold restart)
1. **Fire the 4 fal img2img assets** (F) → QC each → consume → re-render → show user. (User was approving the mock.)
2. **Fix the editorial footer text-collision** (E) — flatten/shorten the band so the closing paragraph clears it; consider adding a dark-text-on-dark-panel check to `qc_dead_space.py`.
3. Decide procedural-marble-CSS vs AI-asset for the footer fill (per the pivot, AI wins; keep the renderer curve+hairline geometry).
4. Then resume **Phase 2** of the treatment plan: port the remaining apex treatments (a4_bi_dashboard, a4_metric_column, a4_two_stack, a4_dark_divider, a4_quote_portrait, a4_side_rail, a4_stacked_hero, a4_portrait_card, glass_card, split_portrait) so all 12 eligible pages render; each via the Porting Procedure + the QC gate + pixel review; then Phase 3 whole-deck render + tuning; Phase 4 guardrails/docs.

---

## STATUS — TREATMENT SYSTEM BUILD (2026-06-18, plan + Phase 0/1 build detail)

Initiative: bake a **TREATMENT SYSTEM** into the renderer so the apex deck (and future decks) render with varied, premium per-page layouts at mixed A3-landscape / A4-portrait formats, to match Richard's `refs/`. Full state in memory **[[treatment-library-state]]** (read it).

- **26 treatments reviewed + user-approved** (16 A3 + 10 A4). The verified visual spec (mockups) is now in the repo: `research/v7-renderer/treatments/refs/html/` (HTML sources) + `treatments/refs/png/` (renders). Screenshotters: `treatments/refs/render_html.py` (1680×1188 A3) and `render_a4.py` (840×1188 A4). Catalogue + each treatment's role/data named in [[treatment-library-state]].
- **Design decisions (user):** scrapped mega-numeral; dropped manifesto; before/after FINAL = full-bleed photo-as-canvas (`fb_uniform`); editorial revised (navy column = full-bleed founder portrait + "Vertrauen von" wall). Replacements kept: dark_stat_hero, glass_card (rep_statcard), split_portrait. Keep ALL 10 A4 styles.
- **Build decisions (user):** bake all 26; **approach A** = responsive templates driven by real page data (NOT fixed embeds); a `treatment_stylist` assigns treatment + A3/A4 format per page by role, no two neighbours alike, **data-gated** (only when the page's data + any required image resolve); A3 = data-gated (the one image-bearing hero case study, ± a data page); **Chromium-only**; `--treatments` flag **default OFF** (= rollback; this tree is NOT git). fal/design_brief explicitly OUT of scope.
- **PLAN v2 (post-audit):** `docs/superpowers/plans/2026-06-18-treatment-system-build.md`. v1 was run through a 4-agent adversarial audit and found **NOT safe** (6 critical, 8 high, 20 med/low); v2 folds in every confirmed fix. **Phase-0 spike PASSED on pixels** (mixed A3 420×297 + A4 210×297 in one Chromium PDF via `prefer_css_page_size=True`). The 6 critical fixes (durable, also in [[treatment-library-state]]): stylist wiring = `assembler.render_package()` after `load_package` (assembler.py:571), NOT render.py; A4 `min-height:261mm` (assembler.py:401)+border spills every A3 page → format-aware min-height + suppress border + assert physical==logical page COUNT on the gs-flattened pdf; named `@page` rules must RE-DECLARE the header/folio margin-box chrome (no inheritance); role→treatment mapping grounded in the real 20-page order; A3 promotion data-gated; the static `test_design_conformance.py` proves nothing about treatments → add a real rasterise+sample check.
- **Verified apex order (20pp):** BYPASS (keep legacy patterns) = ST-01 cover, ST-03 back-cover, 3×ST-31 breathers, 3×ST-07B dark dividers. TREATMENT-ELIGIBLE (12) = ST-02, ST-05, ST-09, ST-14, 5×ST-07A, ST-06, ST-FAZIT, ST-22. Only idx 11 (ST-07A) has a resolved portrait → it's the image-led "hero" (A3); idx 13/14 are adjacent ST-07A (must differ); 4/5 case-study portraits are `miss`.
- **BUILD PROGRESS (2026-06-19): Phase 0 + Phase 1 COMPLETE and verified on pixels.** 42 treatment-system tests green across 8 files. Built so far:
  - **Phase 0:** mixed A3/A4 page-size make-or-break PROVEN through the real Chromium + gs-flatten path (`tests/test_treatment_pagesize.py`: correct sizes + page count + chrome on the flattened PDF). Named `@page a3-landscape`/`@page a4-portrait` + `.page.format-a3`/`.format-a4` routing classes in `assembler.py`. Recursive brand-agnostic + em-dash guard (`tests/test_no_literals_in_architecture.py` now rglobs templates/styles + scans treatment files).
  - **Phase 1:** `treatment_engine.py` = `TreatmentData` + `adapt(page,ctx)` (generic + per-st_type overrides, grounded/no-fabrication, reuses st_07a `_SECTION_SPEC`) + `Treatment` descriptor + `TREATMENTS` registry + `render()` dispatch (template OR render_fn; None on unfit/unknown; graceful CSS load) + `meets_contract`/`candidate_fits`. `treatment_catalog.py` = 12 descriptors. `treatment_stylist.py` = `assign(pages,ctx)`: bypass set, per-st_type best-fit-first candidate config, data-fit gate, deck-wide dedup + no-adjacent-repeat (relaxation order: dedup -> adjacency -> never crash), data-gated A3 hero (ST-07A with resolved image; 1.3a satisfied via data-gating, no fixture change). `assembler.render_package(..., treatments=False)` + `render.py --treatments` (default OFF = rollback) wire it in via `_render_one_page` (treatment-first, raise-safe fallback to legacy, classes stamped only when the treatment actually rendered); default path provably unchanged (swap-test verified). 2 treatments authored + rendered end-to-end: `editorial` (A3, ST-07A hero) + `a4_vertical_timeline` (A4, ST-06/ST-22), `tests/test_treatment_slice.py` renders a 3-page mixed A3+A4 slice clean (verified on pixels vs `treatments/refs/png/`).
  - Verified apex assignment (20pp): bypass=ST-01/ST-03/3xST-31/3xST-07B; treated=12; A3=1 (idx11 editorial hero); 13!=14 holds.
  - **Known cosmetic polish (defer to Phase 2/3 pixel tuning, NOT blockers):** editorial left-field has an upper-middle whitespace void (rhythm); timeline headline not two-tone when the page has no accent keyword (e.g. ST-06).
- **NEXT STEP: Phase 2** = port the remaining apex-used treatments (a4_bi_dashboard, a4_metric_column, a4_two_stack, a4_dark_divider, a4_quote_portrait, a4_side_rail, a4_stacked_hero, a4_portrait_card, glass_card, split_portrait) so all 12 eligible apex pages render via the engine, each via the Porting Procedure + pixel-verified in-deck; then Phase 2.2 the rest of the library; then Phase 3 whole-deck render + tuning; Phase 4 guardrails/docs. The 3 stale `test_render_r2` failures (st_09/st_05/footer) are pre-existing, unrelated to treatments, flagged for separate cleanup (spawn_task task_7231f7ab).

---

## STATUS (2026-06-17 checkpoint, prior — generative pipeline bake-in)

The reference-grounded closed loop (the system fix) is built and largely verified. The APEX deck SHIPS via the polished HAND-BUILT renderer patterns; the reference-template "repurpose" engine is built, proven, and FLAG-GATED (not shipped).

DONE + verified this session (all on the shipped hand-built deck, pixel-checked):
- BAKE-IN PLAN + WIRING GATE (the anti-recurrence mechanism). Plan: docs/superpowers/plans/2026-06-17-bake-in-generative-pipeline.md. Durable memory: [[generative-pipeline-wiring-state]]. The executable source of truth is `research/v7-renderer/tests/test_wiring_conformance.py` — run it FIRST to see what's wired (GREEN) vs the remaining work (RED rows); two anti-regression rows lock in that the shipped deck keeps its social + diagram routing output. PHASE 1 DONE + gate-verified: extracted shared `research/preprocessor/stages/route_package.py` (social placement -> dark-divider cadence -> optional LLM restructure -> diagram proof) now called by BOTH `fixtures/apex/build_package.py` (+ apex-only apply_apex_viz after) and `main.py /render` (manifest=None for now -> data-driven diagram + dark-divider routing reaches production). 3 Phase-1 rows green; 29 preprocessor routing tests pass; committed fixture NOT regenerated (no keys/network). PHASE 2 DONE + gate-verified: `render.py` now takes `--package-dir` (extracted `_build_parser()`; `tests/test_render_cli.py`), so the renderer can rasterize any built package, not just the frozen fixture. PHASE 3 WIRED (offline) + gate-verified: `fixtures/apex/build_package.py` now has a `--fal` flag (`_parse_args`) that calls the real `generate_assets` (Nano Banana) for generate-class imagery + textures instead of `_build_asset_plan`; writes a `.fal_active` marker only on a successful fal build (which arms the provenance gate row); `tests/test_build_package_fal_flag.py`. FAL_KEY + OPENROUTER_API_KEY confirmed present in research/preprocessor/.env. Fixture manifest backed up to `resolved_package.json.prefal.bak`; canonical restore = re-run build_package WITHOUT --fal (deterministic offline). PHASE 3 RUN DONE + gate-verified: the fal build (`build_package.py --fal`) generated 5/5 generate-class assets via Nano Banana (cover_hero, status_quo_scene, fazit_background, background_texture, atmospheric_gradient); ZERO "reused apex fixture image" remain; `.fal_active` written; ALL 8 wiring-gate rows GREEN (including the provenance/anti-cache row). Deck re-rendered clean, fixes intact, fal grounds render behind content, verified on pixels (p1/p4/p18). Fal-build gotchas FIXED (durable, see [[generative-pipeline-wiring-state]]): the --fal httpx client MUST use a long read timeout (httpx.Timeout(300), NOT http_timeout_s=30 → 2K gens ReadTimeout); V7 must be on sys.path for the `fixtures.apex.viz_curation` import; the `report_assets>=4` sanity assertion is now fal-aware (>=1 under --fal). NOTE: fal prompts are currently generic/"stubbed" (no design_brief) → grounds are decorative-but-not-art-directed; a later polish can feed a design_brief for brand-tuned imagery. Remaining: Phase 4 (code-drawn infographic expansion), Phase 5 (textures + dormant assets, swatch-approval gated). Executing autonomously via /loop, gate-verified each. PHASE 4 DONE + verified: code-drawn infographic coverage lifted 6->8 pages (ST-02 stat_strip '30%/60%/30 bis 50%', ST-09 mega_numeral '50%'); the MAGNITUDE+PROCESS viz macros (were stubs) got real bodies+CSS; host wiring on st_02/06/09; grounding guard green (no fabrication). ST-06 step_cascade DISABLED (duplicated the page's own step list + overflowed to a 21st sheet) -> deck back to 20pp, accent budget ok. 46 viz+gate tests green. Phase 5 (textures + dormant assets) is the last phase; its page-texture piece is SWATCH-APPROVAL GATED by the user. PHASE 5 DONE + verified: the light-page GROUND is now a SUBTLE procedural BRAND MARBLE (user-approved swatch C, ~16% over cream; `fixtures/apex/ground_marble.py` GROUND_MARBLE_ALPHA=0.16; blended over cream and written to the background_texture asset, so the existing report_ground_uri mechanism paints it). Mean ground stays warm cream (244/241/235) -> legibility preserved (verified on pixels p3/p5/p19); conformance checks the --color-ground TOKEN (untouched) so it stays green. FAL AI SCENES RIPPED OUT (2026-06-17, user mandate): the 5 fal images rendered as SLOP (Nano Banana literally drew the raw German page text). ROOT CAUSE: the Sonnet prompt-builder `stages/build_image_prompts.py` returns {} without a `design_brief` (apex has none), so generate_assets fell back to dumping page text as the image subject. Also 3/5 were never rendered. FIX: build_package prunes the 4 AI generate-class slots (cover_hero/status_quo_scene/fazit_background/atmospheric_gradient) + the unused extras by slot_id; the deck now uses ONLY real photos (founder, Jousef IG) + the procedural marble ground + code-drawn infographics. `.fal_active` cleared, deck rebuilt OFFLINE (no fal cost), 20pp, 26 gates green. fal stays WIRED (--fal) but OFF until a design_brief is added to art-direct it. OPEN/UNUSED real assets worth wiring next: proof photos (3), testimonial cards (2), case-study-3 portrait. [DONE 2026-06-17: real photos WIRED + pixel-verified — 3 proof photos now render in the ST-05 'Referenzen' row (template elif->if + card-height trim to fit one sheet); case-study-3 real client portrait now wins the ST-07A rail over the social_post phone mockup (st_07a.py: has_social_post gated on `not portrait_uri`); the 2 'testimonial cards' are md5-identical renames of the IG testimonials already shown. Deck 20pp, no overflow, gates green. NEXT per user: (2) build the design_brief so fal imagery is on-brand, then (3) A3 creative canvas.] BAKE-IN COMPLETE: all 5 phases done, all 8 wiring-gate rows green, fal firing, infographics 8/20, marble ground; deck 20pp, accent budget ok. Remaining Phase-5 sub-items are moot/cleanup: the fal build yields only 2 report grounds (both used), and the dead Pillow `composite_device_mockup` is harmless (the founder-scrape device_mockup SLOT path is alive) -> leave or delete in a future cleanup.
- FOOTER A (designed footer, complementary). The deck already runs a HEADER (wordmark top-left + booking URL top-right via the @page top margin boxes), so the footer must NOT duplicate it. Built in `assembler.py shared_head_css`: a full-width TEAL hairline at the content-box bottom (`.page { border-bottom: 0.2mm solid var(--color-accent) }`, mirrors the chromium header top-border; suppressed on st-01/31/32/03 + data-page-mode full-bleed pages) + the page-number folio MOVED from muted `@bottom-left` to navy `@bottom-right` (`var(--color-ink)`, font-head 8pt/600). Weasy folio also moved to `@bottom-right` with a teal border-top. Guard `tests/test_footer.py` (3 tests); 13 green (footer+panel+conformance+literals); verified on pixels (p3/p5/p16 footer bands). Future-optional: a left teal ornament + heavier presence if wanted.
- DARK-PANEL RE-GROUND + invisible-stat bug FIXED (2026-06-16, real-vs-real audit follow-up). Root cause: APEX sets `brand_primary == brand_accent == #4A7C7E`, and EVERY authority panel grounded on `var(--color-primary)` (mid-teal, not a true dark) while ST-05's `IN ZAHLEN` numerals used `var(--color-accent)` -> teal-on-teal -> invisible (labels survived as white). This was ALSO the audit's #1 cross-cutting gap ("dark mass reads mid-value/low-contrast"): our panels were never actually dark. FIX: re-grounded all 7 authority-panel sites (`st_01 .cv-hero`, `st_02 .ol-quote`, `st_05 .ab-lead` + `.ab-stats`, `st_07a .cs-panel`, `st_31 .br-bleed`, `st_fazit .fz-header`) from `--color-primary` -> `--color-ink` (the true dark = `brand_neutral_dark` navy, the token the ST-07A stat rail already used correctly), and lifted `st_05 .ab-stats .c-stat-value` from `--color-accent` -> `--color-on-dark`. New guard `tests/test_panel_contrast.py` (4 tests) binds it; conformance + literals green; re-rendered `--fast` and VERIFIED ON PIXELS: p3 stats now legible white-on-navy, p2/p7/p18 panels read as genuine dark mass. Guardrail saved (memory regressions item 9).
- PENDING (user-approved this turn, not yet built): (i) apex-appropriate DECORATIVE FOOTER deck-wide (Richard has designed footers e.g. aerztepartner marble+gold; ours is a bare page number); (ii) A3 LANDSCAPE SPREAD support for select feature pages (refs mix A4 portrait + A3 landscape: aerz/boss/buch have 1191x842 / 1208x859 / 1202x853 pages; ours is A4-portrait-only) -- architectural, design-first before coding. Real-vs-real audit composites live at `research/v7-renderer/output/cmp/cmp_1..6.png` (6 sections x 4 ref decks; overall ~57% of Richard's bar; wins = typography/text-fidelity/palette, losses = visual-mass/dark-anchoring/terminal-CTA).
- SERIF headlines restored deck-wide (`compile_tokens`: `--font-head` now follows `--font-display`; also fixed the `display_font_embedded` Hiragino false positive).
- ST-07A case-study RAIL fixed: white high-contrast stats, accent only on the rules, readable URL, money figure `>200.000 €` on one clean line.
- ST-07A case study is now ADAPTIVE / page-filling (fixes the dead bottom band on portrait-less case studies like Martina/p7). Root cause: the `cs-fill-root` grid had NO height pin, so it collapsed to content and the rail (align-self stretch) only matched the short left column. FIX in `styles/st_07a.css`: pin the grid to the content box (`min-height: 258mm`, ~3mm safety under the 261mm A4 box so WeasyPrint never spills) + `grid-template-rows: auto 1fr` so the rail runs FULL HEIGHT (stats distribute via space-between) and the `main` row stretches; the narrative column uses `justify-content: space-between` + body at `--type-cta`/1.55 so it breathes evenly instead of pooling whitespace at the bottom. Also fixed a label-detachment bug (the ruled `c-section-label` bottom spacing was larger than the between-section gap, so labels hugged the PREVIOUS paragraph): tightened `c-section-label`/`--rule` bottom spacing in the fill variant. Fills WITH or WITHOUT a photo (a resolved portrait/social-post takes the top of the rail; p12 Frese demonstrates the photo case). No fabrication, no portrait required. Dead-space ~0.15 across all case studies (p7/p9/p12/p14/p15), down from ~0.39; one-page overflow guards + conformance + literals all green (22 passed). VERIFIED ON PIXELS (p7 + p12). Engine note: the overflow guard + loop DET facts render WeasyPrint, hence the 258mm safety + eased type; Chromium (ship) fits comfortably.
- ST-07A response-time metric NO LONGER a text arrow: the "24 Std. -> Minuten" cell now renders Richard's BEFORE/AFTER BAR device (vocab archetype #3) — a gray status-quo bar collapsing to an accent outcome bar, each capped by its verbatim figure + vorher/nachher. Geometry from `_leadnum` (rail bars + viz bars size a value identically); a no-number side floors to a sliver, so NO fabricated figure. Hand-built path (`patterns/st_07a.py` `_transform_bar_heights` + `templates/st_07a.html.jinja` `cs-statstack-ba` + `styles/st_07a.css` `cs-ba-*`, replacing the old `cs-tf-arrow`). Verified on pixels (`output/report-p7.png`); 22 green (conformance + literals guard + ST-07A suite incl. 2 new transform tests). Power BI is NOT in this stack (confirmed) — the device is our native infographic vocabulary. NOTE: the flag-gated template binder (`st_07a_tpl.py`) still dumps the raw value; only matters if `TEMPLATE_DRIVEN_ST07A` is set (apex ships hand-built). Drive-by: removed raw hex from a `layout_types.py` doc comment to clear a pre-existing literals-guard red.
- The "6 von 6" ring overflow KILLED at the source: `fitfs` filter auto-shrinks ring/donut/cluster centre text (+ regression test).
- Vision GRADING wired ON (provider-agnostic `vis_client` endpoint, strong model) + inline Stage 9 converge in `render.py` (`--fast` skips) emitting `convergence_report.json` (the machine verifier).
- N04 overflow/clip deterministic fact LIVE; no false positive on the real deck.
- Conductor knobs: evidence showed NO new knobs warranted (only N08 fires renderer-side, already has its knobs; N01/N15 correctly flagged out-of-renderer).
- Phase 4 repurpose engine BUILT: LayoutTemplate model, VLM extraction (a real niklas ST-07A template), template placement engine, ST-07A binder, calibration (flowing column + dark rail). Faithfully reproduces the niklas composition.

OPEN DECISION (user deferred this at compaction; RE-SURFACE next session). The template engine is NOT yet parity with the hand-built case study for apex (apex has no client portraits, so the fixed-geometry rail leaves dead space; the vision grader fires N08). Three paths:
- (A, RECOMMENDED) Treat the repurpose engine as PROVEN + flag-gated; point the loop at the ORIGINAL goal: run the FULL 20-page LIVE convergence (weasyprint render, vision on) for the complete by-owner defect roadmap, then fix the remaining pages so the apex deck is ready for Richard.
- (B) Invest in template ADAPTIVITY (drop empty regions + redistribute to fill) so the engine reaches parity + becomes the universal renderer.
- (C) Supply/generate client PORTRAIT assets so the rail fills.

KEY ARCHITECTURE FINDINGS (do not relearn):
- The chromium+Ghostscript SHIP pdf has NO extractable text layer (`get_page_text` -> '') and CID fonts with EMPTY names. Text/font DET facts (N03 font, N09 header, N04 overflow, N14 placeholder) therefore work ONLY on the WEASYPRINT render. The loop ALREADY renders weasyprint (brain default), so its DET facts are correct; an earlier "N09 fires 20/20" was an artifact of scanning the chromium pdf.
- ENGINE VIS-FIDELITY decision still open (does NOT block): default plan = keep DET facts on weasyprint, grade the chromium PNGs for the VISUAL/VIS rows (matches ship), move the serif check to the vision grader.
- Run flags: `render.py` default runs Stage 9; `--fast` skips it; `--converge-pages N` / `--converge-max-iter` / `--no-converge-vis` control cost. `TEMPLATE_DRIVEN_ST07A=1` routes ST-07A through the template engine (A/B only; default stays hand-built).

---

# DMC Report — earlier checkpoint detail (per-phase log below)

Read with the memory files (esp. `regressions-and-guardrails`, `richard-design-system`) and the two EXTRACTED specs. Goal: make the APEX deck match Richard's hand-designed `refs/` decks. Renderer: `research/v7-renderer` (Chromium print-to-PDF + Ghostscript flatten). Run: `source .venv/bin/activate; export DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib; python render.py` → `output/report.pdf` + `report-pNN.png`. Targeted tests only (no full suite); NO git.

## Reference-grounded closed loop (NEW initiative, 2026-06-16) — IN PROGRESS
Spec: `docs/superpowers/specs/2026-06-16-reference-grounded-closed-loop-design.md`
Plan: `docs/superpowers/plans/2026-06-16-reference-grounded-closed-loop.md`
This is the fix for "docs documented but not used / a human is the feedback loop": wire `research/quality_loop` into the build so the references DRIVE (Part 4 templates) and CHECK (Parts 1-3 vision loop) the deck. **KEY FINDING:** the vision loop was already wired end to end (`brain` threads `vis_client`; `analysis.score(vis_results=)` adjudicates; reference PNGs exist) but was never RUN with vision on. I had been hand-eyeballing in its place. That is the whole regression source.

**PHASE 1 (vision grading ON) — DONE + VERIFIED 2026-06-16:**
- `vis_client` endpoint now provider-agnostic: keyword `api_base` / `VISION_API_BASE` (env/.env), default OpenRouter. 9/9 offline tests green (`tests/test_vis_client.py`).
- Model = `anthropic/claude-sonnet-4.6` (already a strong vision model; no change). Key in `preprocessor/.env`, never printed.
- LIVE grader smoke (`tools/grader_smoke.py`) PASS: apex cover P01 founder-hero=3 (rationale correctly named the Jousef Murad portrait) vs blank=0; well-formed + discriminates. The grader genuinely reads composition.

**PHASE 2 (inline Stage 9) — DONE + VERIFIED 2026-06-16:**
- `research/quality_loop/stage_converge.py`: `build_report(DeckResult)->dict` + `run_stage(...)` writes `convergence_report.{json,txt}`. 11/11 offline tests green (`tests/test_stage_converge.py` + the vis tests).
- `research/v7-renderer/render.py` wired: runs Stage 9 AFTER `render_package` (Ghostscript is internal to it), skipped by `--fast`; flags `--converge-pages N` / `--converge-max-iter` / `--no-converge-vis`.
- REAL end-to-end proof: full deck rendered (report.pdf, 20pp) + Stage 9 graded the cover LIVE -> well-formed report. The loop INDEPENDENTLY caught **N03 font fallback (HARD-FAIL)** = the unbundled-serif issue (headlines fell back to Source Sans). The machine verifier works.
- **KEY FINDING (high priority):** N03 hard-fails and will gate EVERY page. The brand declares `Gestura Headline` (unbundled); the fallback chain ends in Source **Sans** 3, but the bundled Source **Serif** 4 is the right serif fallback. Fixing the headline fallback chain to land on Source Serif 4 restores the serif-headline signal (THE Richard signal) and clears N03 deck-wide. Do this in Phase 3 (loop can't auto-fix a font; flagged renderer capability gap).
- Observation: `best_reward` scale is large (~9.8e4); harmless to loop correctness (clearing is gated by hard_fail, not reward magnitude). Glance at it in Phase 3.

**PHASE 3a (serif headlines + N03) — DONE + VERIFIED 2026-06-16:**
- ROOT CAUSE of N03: `tokens/compile_tokens.py` pinned `--font-head` to `--font-sans-head` regardless of the `headline_type` axis, so serif brands got SANS headlines on the unbundled `Gestura Headline` face. FIX: `--font-head: var(--font-display)` (axis-aware). Serif brands now render serif headings and the bundled Source Serif 4 actually embeds. 18/18 token tests + a new regression guard.
- VERIFIED ON PIXELS: cover headline "Dein Wachstum FRISST..." and the case-study headline now render SERIF with the teal two-tone keyword (the Richard signal restored); pages well-filled, no overflow.
- N03 was ALSO a perception FALSE POSITIVE: `display_font_embedded` returned False whenever a stray `Hiragino-Sans` glyph fallback appeared, even with Source-Serif-4 present. Fixed to report display-serif presence only (a CJK/symbol glyph fallback is a separate concern). 20/20 analysis+perception tests; `display_font_embedded` now True on the real render -> N03 clears.
- Legit side effect: taller serif headings raised fill, so N08 dead-space no longer fires on the case study; relaxed the brittle render-dependent assertion in `test_analysis.py` honestly (kept N01 hard-fail + cleared=False invariants).
- Minor finding: a glyph falls back to Hiragino-Sans (not in the bundled Source fonts) — a future dedicated glyph-coverage fact, not a hard-fail.

**BIG FINDING — engine mismatch (resolve NEXT):** the convergence loop renders with **weasyprint** (`render_package` default; `brain` passes no engine), but the SHIPPED deck uses **chromium + Ghostscript**, whose flatten leaves **0 extractable fonts** (font DET facts are blind on the shipped PDF). The loop currently grades a DIFFERENT artifact than ships. **REFRAMED 2026-06-16 (investigated):** the pre-flatten chromium `report_print.pdf` DOES exist with fonts, BUT chromium subsets fonts as CID with EMPTY/stripped BaseFont names (`get_page_fonts` -> `''`), so font-NAME detection (the N03 `Source-Serif-4` marker) CANNOT work on the chromium pipeline at all (flattened or pre-flatten). So this is a DESIGN DECISION, not a mechanical fix: grade chromium PNGs for the VISUAL/VIS rows (matches ship), and for the font DET fact either (a) keep a cheap weasyprint render solely for font-name facts, or (b) retire the brittle font-name DET and move "serif headline present" to a VIS question. Cost implication (two renders vs vision). DEFERRED pending that call; not blocking N04.

**USER-FEEDBACK FIX — ST-07A case-study rail (2026-06-16):** user flagged low contrast + the confusing "24 Std. →" arrow on the dark rail. Root cause: rail figures were teal-on-navy (low contrast, the N05 class) and the curated metric value `"24 Std. → Minuten"` was rendered as one oversized numeral (stray arrow). FIX (grounded in the niklas authority-panel reference): figures now WHITE (`--color-on-dark`), accent fires only on the thin cell rules, URL lifted to a readable light teal, numeral glow removed; a metric value containing `→` now renders a proper before->after DEVICE (from dimmed / arrow accent / to bold white) via a new `kind:"transform"` stat in `patterns/st_07a.py` + a template branch + `.cs-statstack-transform` CSS. Backward-compatible (`value` retained for the standard variant's stat_strip). VERIFIED ON PIXELS (p7) + 14/14 st_07a fill-variant tests. Money-figure nit RESOLVED 2026-06-16: rail figures stepped to display (32pt), trailing currency made non-breaking, and a leading comparator (`>`/`<`) now renders as a small dimmed prefix (`.cs-fig-cmp`) so `›200.000 €` reads on one clean line (no orphaned `€`); verified p7.

**PHASE 3b (N04 overflow fact) — DONE + VERIFIED 2026-06-16:**
- `perception.text_overflows_page()` + `PageFacts.content_overflow` (PyMuPDF: any text span beyond the page rect by >2pt = off-page/clipped). Wired as N04's `fact_key` in rubric.py (was None/skipped) + an N04 firing branch in `analysis._negative_fires`. N04 is now a LIVE hard-fail. 22/22 analysis+perception tests; new TDD covers off-page (fires) vs inside (clear); verified NO false-positive on the real deck (cover / ST-09 / case study all clear).
- SCOPE NOTE: N04 catches PAGE-BOUNDS overflow (off-page / clipped). WITHIN-PAGE element overflow (the "6 von 6" ring text past its circle, text past a panel) is best PREVENTED at the viz source (auto-fit), which is the next chunk — detection of that class from a raster is noisy.

**PHASE 3b-ring (viz center auto-fit) — DONE + VERIFIED 2026-06-16:** added a `fitfs` Jinja filter (`templating.py`) that shrinks a ring/donut/cluster CENTER label's font-size by char-count to fit the circle's inner hole; applied as an inline style on the ring/donut/cluster center `<text>` (`viz_transform.jinja` + `viz_proportion.jinja`). "6 von 6" now renders at 16.7px (was 30) and sits INSIDE the ring — verified on a rasterized pixel check + a new regression test. 9 viz_transform + 21 viz tests green. The "6 von 6" bug is killed at the source; short labels ("6/6", "100%") keep the big size.

**PHASE 3c (conductor knobs) — DONE (evidence-based) 2026-06-16:** ran a deterministic defect scan on the deck. KEY FINDING: the chromium+Ghostscript flattened PDF has NO extractable TEXT LAYER (`get_page_text` -> `''`) — so ALL text/font DET facts (N03, N09 header, N04 overflow, N14 placeholder) are blind on the chromium ship; they work correctly on the WEASYPRINT render the loop actually grades (confirmed: `header_furniture_present`=True on weasyprint p1/p7 -> N09 does NOT fire there; the "20/20 N09" was an artifact of scanning the chromium PDF). On weasyprint the ONLY renderer-fixable firing defect is **N08** (dead space), which ALREADY has the density + case_study-layout knobs. N01 (missing client photo) -> asset_gen and N15 (prose stat) -> preprocessor are correctly FLAGGED (out-of-renderer). CONCLUSION: NO new conductor knobs are warranted now (panel/qr/photo/header knobs would be speculative — those defects don't fire); the honest by-owner flag surface is correct. **PHASE 3 COMPLETE.**

**Engine decision (the real open item):** because the chromium ship has no text layer, text/font DET facts are weasyprint-only. Default (unless user overrides): keep DET facts on the weasyprint render (works today), grade the CHROMIUM PNGs for the VISUAL/VIS rows (matches ship), move the serif check to the vision grader. One design call left for grading fidelity; does NOT block Phase 4 build.

**PHASE 4 (reference-derived templates) — IN PROGRESS:** plan at `docs/superpowers/plans/2026-06-16-phase4-reference-layout-templates.md`.
- **Task 1 DONE + VERIFIED:** `v7-renderer/layout_template.py` — `LayoutTemplate` dataclass (Grid + Region[role, x/y/w/h fractions, z, fit] + type_roles + color_roles + whitespace_target + source_refs) with `from_dict` + `validate`: geometry-bounded (region inside the page), ROLE-vocabulary-checked, and BRAND-AGNOSTIC (a hex in a color_role raises). 7/7 TDD (`tests/test_layout_template.py`).
- **Task 2 DONE + VERIFIED:** `quality_loop/references/extract_templates.py` (`build_extract_prompt` brand-agnostic composition-only; `make_vision_extract_fn`; `extract_template` stamps+validates; `build_templates` clusters by st_type + best-exemplar + writes `layout_templates/<st_type>/<variant>.json`) + `vis_client.extract_json` generic vision method (+ FakeVisionClient.extract_json). 14/14 offline tests. LIVE extraction of the niklas ST-07A reference -> a faithful schema-valid template `v7-renderer/layout_templates/ST-07A/case-study-split-stat-rail.json` (eyebrow, ghost_numeral, headline, alternating kicker+body narrative column, portrait + 3 stats in the right rail, rotated footer; color_roles all token ROLE names, no hex). The VLM genuinely extracts Richard's composition as reusable geometry.
- **Task 3 DONE + VERIFIED:** `v7-renderer/patterns/_template_driven.py` — `render_from_template(template, *, render_region)` places each region's HTML in an absolutely-positioned frame at the template's x/y/w/h% (graceful-omit on empty), returns a PageFragment (scoped CSS, no head rules); `render_macro(file, name, **kw)` reaches the existing macro library. Role->page-data BINDING is injectable (per-st_type -> Task 5). 3/3 TDD (geometry %, omit, macro reach).
- **Task 5 IN PROGRESS — binder built + first template render VERIFIED ON PIXELS:** `patterns/st_07a_tpl.py` (`st07a_region_renderer`: role->apex-data binding with per-role occurrence indexing + graceful omit; `render()` loads the niklas template + places via the engine). 2/2 binder TDD. Routed behind env `TEMPLATE_DRIVEN_ST07A` (default deck UNAFFECTED). First pixel render (p7) REPRODUCES the niklas composition with apex content: FALLSTUDIE eyebrow pill, SERIF headline, the alternating section-label+body narrative column (Ausgangssituation/Ziel/Lösung/Ergebnis), the right stat rail (Martina Ammon + 3 stats incl the 24 Std.->Minuten transform), the rotated APEX-CONSULTING.AI footer, ghost numeral. The reference-derived template + binder genuinely produce a faithful page — **Phase 4's approach WORKS.**
- **Task 5 CALIBRATION DONE + VERIFIED ON PIXELS 2026-06-16:** `_calibrate_template` (st_07a_tpl.py) deterministically (a) collapses the kicker+body frames into ONE flowing narrative 'body' region (apex copy now FLOWS, no clipping) and (b) adds a dark full-height 'panel' region behind the right column (the authority rail the v1 extraction missed); rail content (name/lede/stats) renders WHITE on it with teal accent rules. Added 'panel' to REGION_ROLES. 12/12 Phase 4 tests. Re-render (p7) now CLOSELY MATCHES the niklas reference + the hand-built st_07a: dark authority rail with white stats (incl the 24 Std.->Minuten transform), flowing Ausgangssituation/Ziel/Lösung/Ergebnis narrative, serif headline, FALLSTUDIE eyebrow, rotated footer. The reference-derived case study renders faithfully.
- Minor polish (not blocking): empty dark space at the rail TOP (apex has no portrait, so rail content starts mid-panel); ghost numeral faint on the dark rail.
- **GATE RESULT 2026-06-16 (template page NOT swapped — the gate did its job):** conformance 5/5 green, BUT the VISION GRADER on the template-driven p7 vs the niklas reference scored it WEAK (positive-row mean 0.56; **N08 dead-space FIRES**). Root cause: the niklas reference rail has a PORTRAIT filling its top; apex case studies have NO client portrait, so the fixed-geometry template leaves a large EMPTY dark rectangle at the rail top (grader flags N08) and the ghost numeral vanishes on the dark rail. The hand-built `st_07a` handles the missing-portrait case GRACEFULLY (packs the rail) and currently produces a BETTER page for apex's portrait-less content.
- **DECISION:** do NOT swap the apex default — keep the polished hand-built `st_07a` as the shipped case study. The template engine is PROVEN as the reference-repurpose capability (it faithfully reproduced the niklas composition) but needs ADAPTIVE REFLOW (drop empty regions + redistribute siblings to fill) or real portrait assets to reach parity when content doesn't fill the reference. The whole reference-template pipeline (model -> extraction -> engine -> binder -> calibrate -> gate) is BUILT + flag-gated (`TEMPLATE_DRIVEN_ST07A`); the default deck is unchanged (hand-built patterns).
- **PAUSED for a user direction call:** (a) invest in template ADAPTIVITY (reflow/pack empty regions) so it can replace hand-built patterns; (b) supply/generate client PORTRAIT assets so the rail fills; (c) treat the repurpose engine as PROVEN and focus on the apex deck quality + the full LIVE convergence roadmap; (d) other.

## Foundation — DONE + GATED (verified)
- **Palette is Richard's**, in the brand config + render source: cream ground `#F6F3ED`, navy ink `#2E3E50`, muted teal `#4A7C7E`. (`fixtures/apex/brand_input.json` + `resolved_package.json` brand block.)
- **Depth retuned**: all 16 neon-cyan glow shadows → neutral-tone depth. Tasteful depth kept; neon gone.
- **Conformance gate** `tests/test_design_conformance.py` = 6 checks, ALL GREEN: cream / navy / muted-teal / no-neon-glow / layout-variety. This is the executable enforcement of the docs.
- **Layout-type system embedded in code**: `patterns/layout_types.py` (6 types: cover, content, case_study, data, fact_sheet, closing + recipes + ST→layout map).
- Deck renders 20pp, no overflow.

## Layout types — STATUS (corrected 2026-06-16; the old "only case_study rebuilt" note was STALE)
The deck is now BROADLY built across ALL page types (many SWEEP/redesign/DPL/VIZ tasks since that note). Spot-verified on pixels 2026-06-16:
- **case_study (ST-07A, p7/9/12/14/15): REBUILT + ADAPTIVE + verified.** Full-height dark rail that fills the page WITH or WITHOUT a client photo (grid pinned to the content box + space-between distribution; graceful no-box when no portrait); before/after BAR device for transform metrics; serif headline. `styles/st_07a.css` + `patterns/st_07a.py`.
- **cover (ST-01, p1): REBUILT** — founder hero photo, stat callouts (100+ / 30-50%), serif two-tone headline, pills, "in diesem Report" intro. Looks reference-grade.
- **fact_sheet / myths (ST-14, p5): REBUILT** — numbered lies + REALITÄT + sources + a Venn conceptual diagram.
- **breather (ST-31, p6): REBUILT** — full-bleed founder photo + overlaid pull-statement.
- Other pages (about ST-05, data ST-06/22, content ST-02/07B/09, back-cover ST-03, fazit) had design passes (see completed tasks). NOT all individually re-verified on pixels this session.
- **REMAINING renderer work is now POLISH + CONSISTENCY, not "rebuild 5 types from scratch":** a systematic page-by-page pass of all 20 vs `refs/` (best run through the quality loop), the viz flat-on-cream pass, and the few pending component tasks.

## Viz preset library (research/v7-renderer/components/viz_*.jinja + styles/viz.css)
15 presets built + tested (ba_bars, transform_arrow, completion_ring, donut, split_bar, gauge, radial_cluster, icon_array, stat_strip, money_bar, mega_numeral, kpi_card, ranked_bars, step_cascade; + venn in diagram layer). Dispatch `components/viz.jinja`. Curated APEX data (verbatim, grounding-guarded) in `fixtures/apex/viz_curation.py`.
- **Re-themed**: neon glow stripped to neutral depth. Still need a full flat-on-cream pass per the infographic-vocabulary doc.

## KNOWN BROKEN / TODO (do not lose these)
1. **completion_ring center text overflows the circle** ("6 von 6" spilled outside the ring). Ring/donut/cluster centers must auto-fit or be short. FIX before using rings.
2. The 5 non-case-study layout types need rebuilding to the skeletons (see the layout-types widget the user approved + layout_types.py recipes).
3. Every page must carry a matched infographic; image pages shrink text + add a data-viz. Not yet true on all pages.
4. Verification should be a VLM visual diff of our render vs the `refs/` page (the user asked for better reference→design translation). Build/use this as the verify loop, not "it appeared."
5. Brand display font `Gestura Headline` still unbundled (headlines fall back to Source Serif). Serif headline voice is the Richard signal; bundle a serif display face.

## Pages → layout type (from layout_types.py)
cover: ST-01 | content: ST-02/07B/09/31 | fact_sheet: ST-05/14 | data: ST-06/22 | case_study: ST-07A | closing: ST-FAZIT/03.
