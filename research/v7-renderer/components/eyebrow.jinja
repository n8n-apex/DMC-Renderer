{# Small uppercase, letter-spaced label set above a heading.
   --font-head, --type-eyebrow, muted/accent ink. Styling: .c-eyebrow. #}
{% macro eyebrow(text) -%}
<div class="c-eyebrow">{{ text }}</div>
{%- endmacro %}
